// Network/interpolation constants
export const NETWORK = {
	INTERPOLATION_DELAY: 80, // ms — render remote players this far in the past
	SNAPSHOT_BUFFER_SIZE: 20 // max snapshots to keep per remote player
} as const;
